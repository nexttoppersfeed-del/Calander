window.CalanderRecurrence = {expand(event,count=12){if(!event.repeat||event.repeat==="Never")return[event];const out=[];let d=new Date(event.date+"T00:00:00");for(let i=0;i<count;i++){out.push({...event,date:d.toISOString().slice(0,10),seriesId:event.seriesId||event.id});if(event.repeat==="Daily")d.setDate(d.getDate()+1);else if(event.repeat==="Weekly")d.setDate(d.getDate()+7);else if(event.repeat==="Monthly")d.setMonth(d.getMonth()+1);else if(event.repeat==="Yearly")d.setFullYear(d.getFullYear()+1)}return out}};
